# DECISIONS.md

| Date | Decision | Why |
|---|---|---|
| 2026-09-21 | Env via conda, Python 3.12 | tau2-bench needs ≥3.12; system python is 3.9 |
| 2026-09-21 | `litellm==1.82.6` with `--require-hashes` (wheel sha256 `164a3ef3…b205`) | Vendor-audited clean, satisfies tau2's `<1.82.7`; 1.82.7/1.82.8 malicious. **Pending your approval** (CHECKS blocker 2) |
| 2026-09-21 | Thin HTTP client for Jev, not `typesafe-sdk` | Fewer deps, raw response cached for audit |
| 2026-09-21 | Jev confidence for calibration = `probabilities[choice]`; vendor `confidence` logged alongside | Comparable with other deciders' p(choice). **Pending your approval** |
| 2026-09-21 | Agent control via a `Model` wrapper that routes tool choice/stop to the decider, LLM fills arguments | Jev cannot generate; smolagents' seam is `Model.generate` |
| 2026-09-22 | `requirements-gpu.txt`/`setup.sh` install torch/vllm via the **cu130** wheel index, not cu128 as the original plan text said | `dwg-working-freeze.txt` records a verified-working `torch==2.13.0+cu130` combo, and Gilbreth's CUDA 13.1 / driver 590 (CHECKS.md #6) matches cu130, not cu128. Treated as a wording slip in the plan vs. the empirically verified freeze; not re-asked since evidence is unambiguous. **Flag:** if `scripts/verify_env.slurm` fails to resolve the cu130 index, fall back to cu128 and update this row. |
| 2026-09-22 | litellm==1.82.6 install verified by downloading the artifact and checking its sha256 against the known-good hash (CHECKS.md #3), then scanning for `litellm_init.pth`, instead of `pip install --require-hashes` | Full `--require-hashes` would require hash-pinning litellm's entire transitive dependency tree (not just litellm itself), which isn't otherwise needed for this project; the download+hash-check+`.pth`-scan achieves the same "detect the compromised artifact" goal with far less maintenance surface. Logged as a deviation from the literal CHECKS.md suggestion. |
| 2026-09-22 | **RESOLVED (was open conflict), then corrected against a live call.** `JevDecider` calls Jev exclusively through **OpenRouter**, on OpenRouter's dedicated **decisions-model endpoint**: `POST https://openrouter.ai/api/alpha/decisions` (not `/v1/systemone` — that guess was wrong), `Authorization: Bearer $OPENROUTER_API_KEY`, default model `~typesafe/jev-latest` (tilde prefix required for the alias; `typesafe/jev-1.13` also confirmed live for the pinned version). Direct `api.typesafe.ai`/`TYPESAFE_API_KEY` support dropped. Request shape (`state`/`model`/`questions`) matches CHECKS.md #2 as originally documented; response now additionally carries a top-level `id`, `provider: "TypeSafe"`, and **`usage.cost` in dollars per call** (verified live for choice/score/noul), which removes the earlier worry about needing a separate OpenRouter price table for Jev cost tracking. Each `answers[name]` also now carries a `type` field (harmless, ignored by the client). `.env.example` updated accordingly. | Verified against 6 live calls (chat/completions rejection message revealed the real path; then choice/noul/score/alias all round-tripped successfully) using the user's funded OpenRouter key, cost ≈$0.00001–0.000014/call. **Process note:** my first pass wrongly concluded Jev "isn't on OpenRouter" from `/v1/models` (a generic catalog that excludes decisions-type models) and from probing the wrong endpoint — the user's pushback was correct and prompted the real check. |
| 2026-09-22 | **Widened the OpenRouter decision above: ALL frontier LLM calls (OpenAI, Anthropic, Gemini) also go through OpenRouter, not each provider's own API/key.** `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`GEMINI_API_KEY` removed from `.env.example`; `OPENROUTER_API_KEY` is now the only LLM credential in the project. The frontier decider (not yet built) will use LiteLLM's `openrouter/<provider>/<model>` model-id prefix (e.g. `openrouter/openai/gpt-5.6`, `openrouter/anthropic/claude-opus-5`) so LiteLLM still handles retries/cost tracking, just against one backend. `scripts/check_connectivity.py` updated: dropped the separate openai/anthropic/google probes, added a single `openrouter` probe (`GET /api/v1/models`). | User's explicit instruction (2026-09-22): one API for everything, since they've already funded OpenRouter and don't want to manage four separate provider keys/bills. **Flag:** this makes OpenRouter reachability a single point of failure for every hosted decider (previously a blocked provider wouldn't take down the others) — re-run `check_connectivity.py` on a compute node before relying on it in a Slurm job (CHECKS.md #1 is still open on that point). Also: OpenRouter's own price map (not each provider's) will need to be the cost source for LiteLLM's cost tracking, since 1.82.6's bundled price map is provider-native, not OpenRouter-native — recheck when building the frontier decider. |
| 2026-09-22 | **Full project scope re-read from `System1_Jev_Benchmark.pdf`.** It specifies 4 deciders (ceiling=frontier LLM, baseline=Qwen3, floor=a small trained classifier not previously tracked, under-test=Jev) × 3 decision points (router, cache-guard, agent-control), not just agent-control with 3 deciders as earlier memory had it. | Read directly from the reference paper in repo root; earlier phase tracking undercounted both axes. Logged so the smaller scope isn't assumed again. |
| 2026-09-22 | **Agent-control vertical slice complete, live-verified.** Built `src/dwg/jev_model.py` (`JevModel`, a smolagents `Model`) — Jev picks the tool via `JevDecider.choice()`, then a separate `filler_model` (any smolagents `Model`) fills that tool's arguments. Ran `scripts/agent_control_vertical_slice.py`: a toy `ToolCallingAgent` task (add two numbers, then answer) completed correctly through BOTH a plain `LiteLLMModel` (frontier-only) and `JevModel` (Jev decides, cheap OpenRouter model `openai/gpt-oss-20b` fills args) — proves the harness works end-to-end for the agent-control decision point before building router/cache-guard or the other deciders. | User chose "one full vertical slice first" over building all 4 deciders or all 3 harnesses first (2026-09-22 AskUserQuestion), specifically agent-control since its smolagents interface was already researched in CHECKS.md #3/#4. Two real interface bugs found and fixed only by running live, not from docs: (1) smolagents' `ChatMessage.content` must be the block-list form `[{"type":"text","text":...}]`, not a plain string — a plain string only breaks when smolagents' internal message-merging path is hit (two consecutive same-role turns), so it can look fine in isolated testing and still fail live; (2) asking a filler LLM to freehand a JSON object in `content` is unreliable — some models (confirmed: `openai/gpt-oss-20b` via OpenRouter) instead emit a native `tool_call` and leave `content=None`, silently breaking naive JSON parsing. Fixed by constraining the filler to native tool-calling instead (`generate(messages, tools_to_call_from=[tool])`, which smolagents defaults to `tool_choice="required"`), which is both more robust and reuses well-tested framework machinery instead of a custom prompt hack. |
| 2026-09-22 | **CHECKS.md #4/#5 correction: tau2-bench does NOT run on smolagents.** It has its own agent protocol (`tau2.agent.base_agent.HalfDuplexAgent`), completely separate from smolagents' `Model`/`ToolCallingAgent`. Installed tau2-bench (pinned to release tag **v1.0.1**, commit `fc0055d`, not on PyPI — cloned to `$TAU2_REPO_PREFIX`, default `/scratch/gilbreth/$USER/repos/tau2-bench`, `pip install --no-deps`'d into the `dwg` env; `TAU2_DATA_DIR` now exported by `scripts/env.sh`). Built `src/dwg/jev_tau2_agent.py` (`JevTau2Agent`, a `HalfDuplexAgent`) — the same Jev-decides/filler-fills pattern as `JevModel`, reimplemented against tau2's own `Message`/`Tool`/`generate()` API instead of smolagents'. Ran `scripts/tau2_vertical_slice.py` on the real tau2 `mock` domain (real user simulator, real tools, real grading): both tau2's built-in `LLMAgent` (frontier-only) and `JevTau2Agent` (Jev + cheap filler) scored **reward=1.0** on `create_task_1`. Jev's run cost ~2.7x more (2 LLM-shaped calls per turn — Jev + filler — vs. 1) — a real first data point matching the paper's cost/quality trade-off framing, though one task proves nothing statistically. | `examples/agents/` in the tau2-bench repo documents the custom-agent extension point (`HalfDuplexAgent`, factory functions, `run_simulation`) — read directly before assuming smolagents would just plug in, avoiding the mistake logged in [[verify-before-declaring-dead-end]]-style memory (checking assumptions against the actual source before building on them). Added `pandas`/`tenacity`/`deepdiff`/`addict`/`toml` to `requirements-common.txt` (tau2's own missing deps) and a tau2 clone+install step to `setup.sh`, pinned to v1.0.1 specifically — NOT `main`, since v1.0.1's changelog notes a backward-incompatible `banking_knowledge` grading fix that makes older/newer runs non-comparable. `JevModel`/smolagents remains the design for router/cache-guard (no existing benchmark harness to plug into there) and for quick local smoke-testing; `JevTau2Agent` is what actually runs against real tau2-bench data. |
| 2026-09-23 | **Agent-control evaluation redesigned as an A/B matrix with a decision-only arm; arm 0 built and piloted.** Arms (same tau2 tasks, same user simulator, same generator G): **0** decision-only replay (each decider on saved states, nothing executed); **A** Jev decides + G executes; **B** G decides (constrained call) + G executes — A−B isolates the decision-cost difference; **C** G single call (production default; must be reported honestly — the split adds a call); **D** small LLM/classifier decides + G executes (precedent: AgentFlux arXiv 2510.00229, TinyAgent arXiv 2409.00608). Local-hosting framing: Jev has no weights, so "no-API" energy/latency numbers use an open System-1-class stand-in, stated as such. Built: `src/dwg/decisions.py` (Decision, JevChoiceDecider, LLMChoiceDecider, shared `render_transcript`), `src/dwg/stats.py`, `scripts/{collect_decision_states,replay_decisions,analyze_decisions}.py`. | User asked (2026-09-23) to measure the decision stage only, not decision+execution, and for repeated trials with averages/outliers/graphs to handle LLM nondeterminism (the same mock task scored 0.0 then 1.0 on back-to-back runs during onboarding). Fairness choices: every decider gets the **identical rendered transcript** (the LLM as one user message, not tau2's native format); LLM confidence is **verbalized** (known overconfident — say so); cost for both comes from **OpenRouter's own `usage.cost`**; calls are shuffled across deciders so network conditions hit both equally; CIs are **cluster bootstrap over states** (repeats on one state are correlated). Labels = the reference agent's action at states from **reward=1 episodes only**, which is a lower bound: the pilot showed both deciders' largest "error" (7 states each) is choosing `get_users` before `create_task` where the reference went straight to `create_task` — a valid path. **Flag:** needs multi-reference or gold-action-aware labels before accuracy numbers go in the paper. Also fixed: `JevTau2Agent` previously rendered tool-call turns as `assistant: None`, hiding prior tool calls from Jev. |
| 2026-09-23 | **Arm E added: Kev as the open-weights, locally hosted "Jev-like" decider.** Kev (github.com/jaredpalmer/kev, Apache-2.0, released 2026-09-17, ~5.6k stars; pinned to commit `557598f`, weights as updated 2026-09-21) — Qwen3.5 + LoRA + pointer head, 0.8B/4B/9B, speaks TypeSafe's System One wire format at `/v1/systemone`, so it reuses `JevDecider` with only a different endpoint (`make_kev_decider`). Authors report Kev-9B 0.822 vs Jev 0.857 on their new-source dev set (not a controlled comparison — Jev's training data is unknown). Runs in **its own uv env** (pins `torch<2.9`; dwg pins torch 2.13 for vLLM), served by `scripts/serve_kev.sh`; Gilbreth job `scripts/kev_replay.slurm`. Energy: **NVML cumulative energy counter** (`nvmlDeviceGetTotalEnergyConsumption`, mJ) read around each call, not the ~10 Hz `nvidia-smi` polling CHECKS.md #8 planned — exact over 100–500 ms windows; whole-GPU, so metered calls are serialized by a lock and Gilbreth runs use `--exclusive`; gross and idle-subtracted net J/decision both reported. `KEV_PREFIX_CACHE=0` by default in our launcher (Kev's default is 4). | User asked (2026-09-23) for a 5th test using an open-source Jev-like model released after Jev, to take API cost/overhead out and measure the model itself (latency + energy on local hardware). Chosen over other clones checked the same day: OpenJev (sources disagree on size/license: 27B CC-BY-NC vs. 26B Apache), Decider/Mapika (Apache-2.0, same API, but lower JevBench rank), Laya (421M encoder, CPU — a candidate floor point, not a Jev stand-in). **Framing for the paper:** Kev is a third-party reproduction, not Jev's weights — its results speak for the System-One paradigm run locally, not for Jev. Prefix cache off because replay repeats the identical state R times; cache hits on repeats would understate Kev's per-decision cost versus a real agent loop, where each state is new. Kev's server reports `latency_ms`, so client latency splits into model time vs. HTTP overhead. `JevDecider(api_key="")` now means "send no key" (previously `""` fell through to `OPENROUTER_API_KEY`, which would have sent the OpenRouter key to a local server). |
| 2026-09-23 | **Kev-0.8B laptop pilot run; energy method hardened after the laptop GPU's counter proved unreliable.** Kev-0.8B on the RTX 3050 Ti (4 GB) needed `KEV_MERGE=0` (bf16 base + unmerged LoRA; the default merged path loads fp32 first and OOMs on 4 GB) — a laptop-only deviation; Gilbreth runs use Kev's default merged path. Pilot result (65 mock states × 5): Kev-0.8B p50 latency **0.131 s** (0.127 s model, 0.004 s HTTP) vs Jev 0.374 s vs gpt-oss-20b 0.873 s, but accuracy **0.354** [0.246, 0.477] vs Jev 0.772 — it almost never picks `respond_to_user` after the action already ran (190 of its 210 errors). Not a truncation issue (Kev serves up to 8192 state tokens; ours are ~430). Kev's own 4B model card already shows large out-of-distribution gaps vs Jev (MMLU-Pro sample 0.468 vs 0.835), so 0.8B is not an accuracy stand-in for Jev; 4B/9B on Gilbreth decide that. Energy: added `GpuEnergyMeter.validate()` (tight-poll counter vs NVML power reading), 5 discarded **warmup** calls per local decider (cold first call 0.65 s vs 0.11 s steady), and **block energy** (counter delta over a contiguous metered-only run / calls) as the headline J/decision, since the counter steps every ~100 ms and per-call windows catch 0–1 steps. | On the laptop GPU (driver 580.173.02) the NVML energy counter reads a plausible 11 W across a 5 s sleep but accumulates ~700 W under tight polling — physically impossible — so the validation now disables energy there (recorded in `meta.json` as `energy_unavailable`) and laptop numbers are latency-only. The A100 counter is documented as accurate, but the same check runs there rather than being assumed. Figures: decider colors now pinned by family (jev/LLM/Kev = palette slots 1/2/3) — adding Kev had repainted the LLM series, which the dataviz rules forbid. |
