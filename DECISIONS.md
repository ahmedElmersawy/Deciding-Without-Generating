# DECISIONS.md

| Date | Decision | Why |
|---|---|---|
| 2026-09-21 | Env via conda, Python 3.12 | tau2-bench needs ≥3.12; system python is 3.9 |
| 2026-09-21 | `litellm==1.82.6` with `--require-hashes` (wheel sha256 `164a3ef3…b205`) | Vendor-audited clean, satisfies tau2's `<1.82.7`; 1.82.7/1.82.8 malicious. **Pending your approval** (CHECKS blocker 2) |
| 2026-09-21 | Thin HTTP client for Jev, not `typesafe-sdk` | Fewer deps, raw response cached for audit |
| 2026-09-21 | Jev confidence for calibration = `probabilities[choice]`; vendor `confidence` logged alongside | Comparable with other deciders' p(choice). **Pending your approval** |
| 2026-09-21 | Agent control via a `Model` wrapper that routes tool choice/stop to the decider, LLM fills arguments | Jev cannot generate; smolagents' seam is `Model.generate` |
| 2026-09-22 | `requirements-gpu.txt`/`setup.sh` install torch/vllm via the **cu130** wheel index, not cu128 as the original plan text said | `dwg-working-freeze.txt` records a verified-working `torch==2.13.0+cu130` combo, and Gilbreth's CUDA 13.1 / driver 590 (CHECKS.md #6) matches cu130, not cu128. Treated as a wording slip in the plan vs. the empirically verified freeze; not re-asked since evidence is unambiguous. **Flag:** if `scripts/verify_env.slurm` fails to resolve the cu130 index, fall back to cu128 and update this row. |
| 2026-09-22 | litellm==1.82.6 install verified by downloading the artifact and checking its sha256 against the known-good hash (CHECKS.md #3), then scanning for `litellm_init.pth`, instead of `pip install --require-hashes` | Full `--require-hashes` would require hash-pinning litellm's entire transitive dependency tree (not just litellm itself), which isn't otherwise needed for this project; the download+hash-check+`.pth`-scan achieves the same "detect the compromised artifact" goal with far less maintenance surface. Logged as a deviation from the literal CHECKS.md suggestion. |
| 2026-09-22 | **RESOLVED (was open conflict), then corrected against a live call.** `JevDecider` calls Jev exclusively through **OpenRouter**, on OpenRouter's dedicated **decisions-model endpoint**: `POST https://openrouter.ai/api/alpha/decisions` (not `/v1/systemone` — that guess was wrong), `Authorization: Bearer $OPENROUTER_API_KEY`, default model `~typesafe/jev-latest` (tilde prefix required for the alias; `typesafe/jev-1.13` also confirmed live for the pinned version). Direct `api.typesafe.ai`/`TYPESAFE_API_KEY` support dropped. Request shape (`state`/`model`/`questions`) matches CHECKS.md #2 as originally documented; response now additionally carries a top-level `id`, `provider: "TypeSafe"`, and **`usage.cost` in dollars per call** (verified live for choice/score/noul), which removes the earlier worry about needing a separate OpenRouter price table for Jev cost tracking. Each `answers[name]` also now carries a `type` field (harmless, ignored by the client). `.env.example` updated accordingly. | Verified against 6 live calls (chat/completions rejection message revealed the real path; then choice/noul/score/alias all round-tripped successfully) using the user's funded OpenRouter key, cost ≈$0.00001–0.000014/call. **Process note:** my first pass wrongly concluded Jev "isn't on OpenRouter" from `/v1/models` (a generic catalog that excludes decisions-type models) and from probing the wrong endpoint — the user's pushback was correct and prompted the real check. |
| 2026-09-22 | **Widened the OpenRouter decision above: ALL frontier LLM calls (OpenAI, Anthropic, Gemini) also go through OpenRouter, not each provider's own API/key.** `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`GEMINI_API_KEY` removed from `.env.example`; `OPENROUTER_API_KEY` is now the only LLM credential in the project. The frontier decider (not yet built) will use LiteLLM's `openrouter/<provider>/<model>` model-id prefix (e.g. `openrouter/openai/gpt-5.6`, `openrouter/anthropic/claude-opus-5`) so LiteLLM still handles retries/cost tracking, just against one backend. `scripts/check_connectivity.py` updated: dropped the separate openai/anthropic/google probes, added a single `openrouter` probe (`GET /api/v1/models`). | User's explicit instruction (2026-09-22): one API for everything, since they've already funded OpenRouter and don't want to manage four separate provider keys/bills. **Flag:** this makes OpenRouter reachability a single point of failure for every hosted decider (previously a blocked provider wouldn't take down the others) — re-run `check_connectivity.py` on a compute node before relying on it in a Slurm job (CHECKS.md #1 is still open on that point). Also: OpenRouter's own price map (not each provider's) will need to be the cost source for LiteLLM's cost tracking, since 1.82.6's bundled price map is provider-native, not OpenRouter-native — recheck when building the frontier decider. |
