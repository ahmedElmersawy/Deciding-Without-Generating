# Copy to .env (git-ignored). Leave blank to run open-baseline only.
TYPESAFE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
HF_TOKEN=            # needed for GAIA (gated dataset)
# TYPESAFE_API_BASE=https://api.typesafe.ai
# HTTPS_PROXY=       # only if compute nodes lack egress
